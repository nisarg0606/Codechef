#include <bits/stdc++.h>
using namespace std;

#define ll long long int
int main(int argc, char const *argv[])
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll n, k;
        cin >> n >> k;
        string s;
        cin >> s;
        vector<ll> v(k);
        // char c[K];
        int j;
        cin >> j;
        v.push_back(j);
        for (int i = 1; i <= k; i++)
        {
            
        }
        
    }

    return 0;
}
